package com.example.parcial_1.model;

/** Categoría del producto para filtros y reglas simples. */
public enum Category {
    PRINT,      // impresiones (blanco/negro, color, copias)
    BINDING     // encuadernados/anillados
}
